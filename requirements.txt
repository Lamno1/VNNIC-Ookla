# Core dependencies used by the acquisition and validation scripts.
pandas>=2.2,<3
pyarrow>=15,<23
numpy>=1.26,<3
scipy>=1.12,<2
matplotlib>=3.8,<4
psutil>=5.9,<7

# The full connectivity_enterprise pipeline should be frozen with the
# run's environment.json artifact until a complete lock file is published.
